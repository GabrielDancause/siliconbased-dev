{
  "name": "siliconbased-dev",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate:chmod": "node scripts/generate-chmod-pages.cjs",
    "generate:pageweight": "node scripts/crawl-page-weight.cjs",
    "generate:sourdough": "node scripts/crawl-sourdough.cjs && node scripts/inject-sourdough-data.cjs",
    "generate:tax-brackets": "node scripts/crawl-tax-brackets.cjs && node scripts/generate-tax-brackets-html.cjs"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "smol-toml": "^1.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "jsdom": "^28.1.0",
    "playwright": "^1.58.2"
  }
}
